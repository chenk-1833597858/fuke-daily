log_level: INFO

storage:
  performance:
    max_search_threads: 2

service:
  http_port: 6333
  grpc_port: 6334

# Enable fastembed for on-device embedding
fastembed:
  enable: true
  model: BAAI/bge-small-zh-v1.5
